{{#if posts.length}}
<section>
  <h2>Your Posts</h2>
  <ul class="list-group">
    {{#each posts as |post|}}
    <li class="list-item">
      <a href="/dashboard/edit/{{post.id}}">
        <h3> {{post.title}}</h3>
      </a>
    </li>
    {{/each}}
  </ul>
</section>
{{/if}}